---
interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;
---

<div id="mobileMenu" class="lg:hidden hidden px-4 pb-4 shadow-md">
  <a
    href="/"
    class={`block py-3 text-center text-lg hover:text-[var(--color-primary-dark)] font-medium ${currentPath === "/" ? "text-[var(--color-primary)] [font-weight:600]" : ""}`}
    >Home</a
  >
  <a
    href="/shop"
    class={`block py-3 text-center text-lg hover:text-[var(--color-primary-dark)] font-medium ${currentPath.startsWith("/shop") ? "text-[var(--color-primary)] [font-weight:600]" : ""}`}
    >Wellness Shop</a
  >
  <a
    href="/blog"
    class={`block py-3 text-center text-lg hover:text-[var(--color-primary-dark)] font-medium ${currentPath.startsWith("/blog") ? "text-[var(--color-primary)] [font-weight:600]" : ""}`}
    >Health Education</a
  >
</div>
