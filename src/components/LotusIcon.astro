---
import type { ImageMetadata } from "astro";
import lotus from "../assets/lotus.svg";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<img src={lotus.src} alt="Lotus" class={className} />
