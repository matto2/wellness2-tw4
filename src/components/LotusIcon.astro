---
import type { ImageMetadata } from "astro";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<img src="/lotus-warm.svg" alt="Lotus" class={className} />
